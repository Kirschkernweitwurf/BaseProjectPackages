namespace Tracking
{
    /// <summary>
    /// Defines priority levels for any tracking use case.
    /// </summary>
    public enum EPriority : byte
    {
        Low = 0,
        Medium = 1,
        High = 2,
        Critical = 3
    }
}